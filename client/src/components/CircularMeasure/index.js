import React, { Component } from 'react'

import './style.css'

class CircularMeasure extends Component {
  render () {
    const { color } = this.props
    const { measure } = this.props

    /* 
     * Cf. tutoriel sur les jauges circulaire en canva sur AlsaCréation 
     * /!\ ce n'est pas un tuto React, il faut penser à adapter (création de components si nécessaire)
     */

    return (
      <div>
      </div>
    )
  }
}

export default CircularMeasure